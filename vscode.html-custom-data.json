{
  "$schema": "https://raw.githubusercontent.com/microsoft/vscode-html-languageservice/main/docs/customData.schema.json",
  "version": 1.1,
  "tags": [
    {
      "name": "cui-article-filter",
      "description": "Filters a sibling or selector-targeted list of article cards by category.\nReads `data-category` from each `.cui-article-card` and shows/hides cards\nbased on which radio/checkbox inputs inside the filter are checked.\n---\n\n\n### **Events:**\n - **cui-article-filter-change** - Fires when the visible-category set changes. `detail.selected` is the array of currently checked values.\n\n### **Methods:**\n - **refresh(): _void_** - Re-read children and recompute visibility. Call after dynamic updates.\n\n### **Slots:**\n - _default_ - Radio or checkbox inputs (typically wrapped in labels). Each input's `value` is matched against the `data-category` on the target cards.",
      "attributes": [
        {
          "name": "target",
          "description": "CSS selector for the element containing the cards. If omitted, the filter will look for a sibling `.cui-article-grid` or `.cui-article-list` to filter.",
          "values": []
        }
      ],
      "references": []
    },
    {
      "name": "cui-carousel",
      "description": "Flexible content carousel. Slides are slotted light-DOM elements, so every\nslide stays crawlable regardless of which one is visible. Compose each slide\nfrom any markup — an `<img>` / `<video>` background with overlaid text and\nbuttons (e.g. `.cui-media-overlay`), or a plain content panel. A separate\n`fixed` slot holds chrome that persists across every slide (brand mark,\npersistent CTA). Supports slide / fade transitions, autoplay with a\npause control, looping, keyboard navigation, and `prefers-reduced-motion`.\n---\n\n\n### **Events:**\n - **cui-carousel-change** - Fires after the active slide changes.\n\n### **Methods:**\n - **next(): _void_** - Advance to the next slide (wraps when `loop` is on).\n- **prev(): _void_** - Return to the previous slide (wraps when `loop` is on).\n- **go(target: _number_, options: _{ focus?: boolean }_): _void_** - Move to a specific slide index.\n- **play(): _void_** - Start autoplay (no-op when reduced motion is preferred).\n- **pause(): _void_** - Pause autoplay.\n\n### **Slots:**\n - **slide** - One element per slide. Any content works; use `.cui-media-overlay` for media with overlaid copy and buttons.\n- **fixed** - Content overlaid on top of every slide and pinned in place while slides change (logo, persistent call-to-action). Buttons here can carry `data-cui-carousel-prev` / `-next` / `-goto=\"<i>\"` to drive the carousel; elements anywhere else on the page can do the same by adding `data-cui-carousel-target=\"<id>\"`.\n\n### **CSS Parts:**\n - **viewport** - The clipping region around the slides.\n- **track** - The slot wrapping the slides (the moving track).\n- **fixed** - The persistent overlay layer.\n- **controls** - The previous / next arrow container.\n- **previous** - The previous-slide button.\n- **next** - The next-slide button.\n- **pagination** - The dot pagination container.\n- **dot** - A single pagination dot.",
      "attributes": [
        {
          "name": "index",
          "description": "The active slide index (zero-based). Reflected.",
          "values": []
        },
        {
          "name": "autoplay",
          "description": "Advance slides automatically.",
          "values": []
        },
        {
          "name": "interval",
          "description": "Autoplay delay in milliseconds. Defaults to 5000.",
          "values": []
        },
        {
          "name": "loop",
          "description": "Wrap past the ends. Defaults to on; set `loop=\"false\"` to clamp at the first / last slide and disable the wrap.",
          "values": []
        },
        {
          "name": "transition",
          "description": "Transition style. Defaults to `slide`.",
          "values": [
            { "name": "slide" },
            { "name": "fade" },
            { "name": "none" }
          ]
        },
        {
          "name": "controls",
          "description": "Show the previous / next arrows. Defaults to on; set `controls=\"false\"` to hide them.",
          "values": []
        },
        {
          "name": "pagination",
          "description": "Show the dot pagination. Defaults to on; set `pagination=\"false\"` to hide it.",
          "values": []
        },
        {
          "name": "pause-on-hover",
          "description": "Pause autoplay on hover / focus. Defaults to on; set `pause-on-hover=\"false\"` to keep playing.",
          "values": []
        }
      ],
      "references": []
    },
    {
      "name": "cui-cookie-banner",
      "description": "Cookie consent banner. Renders a fixed bottom-anchored card with an\naccept-all / necessary-only / settings flow, persists the choice in a\ncookie, and emits a typed `cui-cookie-consent-change` event so the host\napp can load (or block) analytics and marketing scripts. The component is\ndeliberately tracker-free — it manages consent and UI only; you wire the\nactual scripts in response to the event.\n\nOn connect it reads the stored cookie: when absent the banner opens; when\npresent it stays hidden and re-emits the saved consent with\n`reason: \"restore\"` so consumers can re-apply it on every page load.\n\nAll copy is provided through named slots with English defaults, so the\nbanner is fully translatable and brandable without touching the component.\n---\n\n\n### **Events:**\n - **cui-cookie-banner-open** - Fires after the banner opens.\n- **cui-cookie-banner-close** - Fires after the banner closes.\n- **cui-cookie-consent-change** - Fires whenever consent is saved, revoked, or restored from the cookie.\n\n### **Methods:**\n - **revoke(): _void_** - Clear the stored choice and re-open the banner.\n\n### **Slots:**\n - **title** - Banner heading. Default \"Cookie settings\".\n- **description** - Intro paragraph explaining cookie use.\n- **accept-label** - Accept-all button text. Default \"Accept all\".\n- **decline-label** - Necessary-only button text. Default \"Necessary only\".\n- **settings-label** - Open-settings button text. Default \"Settings\".\n- **save-label** - Save-preferences button text. Default \"Save preferences\".\n- **necessary-label** - / necessary-description - Necessary category copy.\n- **analytics-label** - / analytics-description - Analytics category copy.\n- **marketing-label** - / marketing-description - Marketing category copy.\n- **policy** - Cookie policy link. Default points to `/privacy-policy`.",
      "attributes": [
        {
          "name": "open",
          "description": "Reflects banner visibility. Set or remove to show/hide imperatively.",
          "values": []
        },
        {
          "name": "categories",
          "description": "Comma-separated optional categories to show (subset of `analytics,marketing`). Default shows both. `necessary` is always present and locked on.",
          "values": []
        },
        {
          "name": "manual",
          "description": "When present, the banner does not auto-open on connect even without a stored choice. Call `show()` to open it yourself.",
          "values": []
        },
        {
          "name": "cookie-name",
          "description": "Name of the consent cookie. Default `cui-cookie-consent`.",
          "values": []
        },
        {
          "name": "cookie-expiry",
          "description": "Cookie lifetime in days. Default `365`.",
          "values": []
        }
      ],
      "references": []
    },
    {
      "name": "cui-button",
      "description": "Renders a `<button>` or `<a>` with shared visual styling and disabled state\nhandling. Choose anchor mode by setting the `href` attribute, and button mode by omitting it.\n---\n",
      "attributes": [
        {
          "name": "disabled",
          "description": "= Disables the underlying control. For anchor mode this also prevents navigation.",
          "values": []
        },
        {
          "name": "href",
          "description": "When set, the button renders as an anchor (`<a>`) instead of a `<button>`. The value is applied to the anchor's `href` attribute.",
          "values": []
        },
        {
          "name": "target",
          "description": "Anchor target (e.g. `_blank`). Only applies when `href` is set.",
          "values": []
        },
        {
          "name": "rel",
          "description": "Anchor relationship. Only applies when `href` is set.",
          "values": []
        },
        {
          "name": "type",
          "description": "Button type. Only applies when `href` is not set. Defaults to `\"button\"`.",
          "values": [
            { "name": "button" },
            { "name": "submit" },
            { "name": "reset" }
          ]
        }
      ],
      "references": []
    },
    {
      "name": "cui-calendar",
      "description": "Month-view calendar that decorates a slotted list of\n`.cui-event-card` children. Reads each card's first `<time\ndatetime>` to plot an event pill onto the matching day cell;\nthe source cards remain in light DOM beneath the grid (or\nvisually hidden via `events-hidden`) so search engines and feed\nreaders index the full event copy without depending on the\ncomponent's JS.\n\nClick a day to fire `cui-calendar-day-select`; click an event\npill to fire `cui-calendar-event-select`; the prev / next\nbuttons fire `cui-calendar-navigate`. Keyboard nav inside the\ngrid uses arrow keys, Home / End, and PgUp / PgDn (with Shift\njumping a year).\n---\n\n\n### **Events:**\n - **cui-calendar-event-select** - Fires when an event pill is activated.\n- **cui-calendar-day-select** - Fires when a day cell is activated.\n- **cui-calendar-navigate** - Fires when the visible month changes.\n\n### **Methods:**\n - **goTo(year: _number_, month: _number_): _void_** - Programmatically navigate to a specific month (1-12).\n- **next(): _void_** - Move forward one month.\n- **previous(): _void_** - Move back one month.\n- **refresh(): _void_** - Re-read slotted event cards. Call after dynamic updates.\n\n### **Slots:**\n - _default_ - One or more `.cui-event-card` elements. Cards without a `<time datetime>` are skipped; multi-day events use the start date for plotting (single-cell rendering for v1).",
      "attributes": [
        {
          "name": "year",
          "description": "Initial visible year (e.g. `\"2026\"`). Defaults to today.",
          "values": []
        },
        {
          "name": "month",
          "description": "Initial visible month (1-12). Defaults to today.",
          "values": []
        },
        {
          "name": "weekday-start",
          "description": "First day of the week. Defaults to `\"monday\"`.",
          "values": [{ "name": "monday" }, { "name": "sunday" }]
        },
        {
          "name": "locale",
          "description": "BCP-47 locale used for month / weekday labels. Defaults to the document's `lang` or `navigator.language`.",
          "values": []
        },
        {
          "name": "events-hidden",
          "description": "Visually hide the source event list while keeping it in the DOM for accessibility and SEO.",
          "values": []
        }
      ],
      "references": []
    },
    {
      "name": "cui-day-planner",
      "description": "Vertical day planner — a single-day timeline that decorates a\nslotted list of `.cui-event-card` children. Events are placed\nby start time, sized by duration, and column-packed so\nconcurrent events sit side-by-side. The source cards remain in\nlight DOM beneath the board (or visually hidden via\n`events-hidden`) so search engines and feed readers index the\nfull event copy without depending on the component's JS.\n\nReads each card's first `<time datetime>` for the start; an\noptional second `<time datetime>` or a `data-end` attribute\n(full ISO or `HH:MM`) provides the end. Cards without an\nexplicit end get a default `60`-minute duration.\n---\n\n\n### **Events:**\n - **cui-day-planner-event-select** - Cancelable; fires when an event block is activated.\n- **cui-day-planner-slot-select** - Fires when a row (empty slot) is activated.\n- **cui-day-planner-navigate** - Fires after the visible day changes.\n\n### **Methods:**\n - **goTo(target: _string | Date_): _void_** - Programmatically jump to a date (YYYY-MM-DD or Date).\n- **next(): _void_** - Move forward one day.\n- **previous(): _void_** - Move back one day.\n- **refresh(): _void_** - Re-read slotted event cards. Call after dynamic updates.\n\n### **Slots:**\n - _default_ - `.cui-event-card` elements whose first `<time>` falls on the visible date. Cards on other days are placed in the slot but skipped from the timeline.",
      "attributes": [
        {
          "name": "date",
          "description": "Visible day in `YYYY-MM-DD`. Defaults to today.",
          "values": []
        },
        {
          "name": "start-hour",
          "description": "Visible window start hour (0-23). Defaults to `7`.",
          "values": []
        },
        {
          "name": "end-hour",
          "description": "Visible window end hour (1-24). Defaults to `22`.",
          "values": []
        },
        {
          "name": "slot-minutes",
          "description": "Minor-gridline cadence. Defaults to `60`.",
          "values": [{ "name": "15" }, { "name": "30" }, { "name": "60" }]
        },
        {
          "name": "locale",
          "description": "BCP-47 locale for the day-header label. Defaults to the document's `lang` or `navigator.language`.",
          "values": []
        },
        {
          "name": "now-line",
          "description": "Render the now-line when viewing today. Defaults to present.",
          "values": []
        },
        {
          "name": "events-hidden",
          "description": "Visually hide the slotted card list while keeping it in the DOM.",
          "values": []
        }
      ],
      "references": []
    },
    {
      "name": "cui-disclosure",
      "description": "Lightweight custom element that wraps a native `<details>` element so it can\nbe opened, closed, or toggled by ID from anywhere on the page, and so its\nopen state is observable as an attribute and a custom event.\n\nThe `<details>` element stays in the light DOM, so it's contents remain\nsearchable, indexable, and stylable from the host page.\n---\n\n\n### **Events:**\n - **cui-disclosure-toggle** - Bubbles after the disclosure opens/closes. `detail.open` is the new state.\n\n### **Slots:**\n - _default_ - A single `<details>` element with its `<summary>` and body. The element does not render its own UI - it only observes and controls the nested `<details>`.",
      "attributes": [
        {
          "name": "open",
          "description": "Reflects the inner `<details>.open`. Set or remove this attribute to open/close the disclosure; the attribute also updates when the user toggles the disclosure by clicking the `<summary>` directly.",
          "values": []
        }
      ],
      "references": []
    },
    {
      "name": "cui-code",
      "description": "Displays a code sample with a copy-to-clipboard button. Reads source from\nits text content, from an HTML comment child, or from a `<script type=\"…\">`\nchild — whichever is present — so example markup can stay valid HTML.\n---\n\n\n### **Slots:**\n - _default_ - The code to display. Can be raw text, an HTML comment containing markup, or a `<script type=\"text/plain\">` block (useful when the example itself is HTML you don't want the browser to render).",
      "attributes": [
        {
          "name": "language",
          "description": "Source language hint (`text`, `html`, `css`, `js`, …). Used as a CSS hook and in the copy-button label.",
          "values": []
        }
      ],
      "references": []
    },
    {
      "name": "cui-field",
      "description": "Form-field wrapper that surfaces native `ValidityState` as user-visible\nmessages, supports server-side errors via `setError()`, async validators\nvia `addValidator()`, and i18n via slotted `error-<validityKey>` content.\nDesigned to be a child of `cui-form`, but works standalone.\n---\n\n\n### **Events:**\n - **cui-field-valid** - Fires when the field transitions to a valid state.\n- **cui-field-invalid** - Fires when validation fails. `detail.key` is the first failing `ValidityState` key; `detail.params` carries control-derived values for message interpolation.\n\n### **Slots:**\n - _default_ - The form control (`<input>`, `<select>`, `<textarea>`, or a custom control matching `CuiFieldControl`).\n- **label** - The field label. Will be associated with the control via `for=` / `aria-labelledby`.\n- **help** - Descriptive help text shown beneath the control.\n- **error** - Generic error message. Used when no `error-<key>` slot matches the failing validity key.\n- **error-{validityKey}** - Validity-key-specific error message (`error-valueMissing`, `error-typeMismatch`, `error-tooShort`, …). The first matching slot wins.",
      "attributes": [
        {
          "name": "required",
          "description": "Mirrors the control's `required` attribute and marks the field visually as required.",
          "values": []
        }
      ],
      "references": []
    },
    {
      "name": "cui-form",
      "description": "Coordinates submission and validation across child `cui-field` elements.\nRuns each field's async validators, blocks submit on failures, and\nsurfaces submit / success / error events with typed details. Set a JS\nsubmit handler with `setSubmitHandler(fn)`.\n---\n\n\n### **Events:**\n - **cui-form-invalid** - Fires when submission is blocked because one or more fields failed validation.\n- **cui-form-submit** - Fires when the form passes validation and is about to submit. `detail.formData` is the `FormData` snapshot.\n- **cui-form-success** - Fires after a JS submit handler resolves successfully.\n- **cui-form-error** - Fires when a JS submit handler rejects. `detail.error` is the thrown value.\n\n### **Slots:**\n - _default_ - A native `<form>` element containing `cui-field`s and a submit button. The form's `action` and `method` are honored when no JS handler is attached.",
      "attributes": [],
      "references": []
    },
    {
      "name": "cui-map",
      "description": "Leaflet-backed interactive map with clustered points, GeoJSON regions,\ntheme-aware tiles, and a lazy-loaded library bundle. Points and regions\nare declared as light-DOM children so content stays SEO-visible and\nserver-renderable.\n---\n\n\n### **Events:**\n - **cui-map-ready** - Fires once the library has loaded and the map is interactive.\n- **cui-map-bounds-change** - Fires after the user pans or zooms.\n- **cui-map-point-click** - Fires when a marker is clicked.\n- **cui-map-region-click** - Fires when a GeoJSON region is clicked.\n\n### **Slots:**\n - _default_ - Fallback content shown before the library loads or when JS is disabled (e.g. a static `<address>` list).\n- **title** - Accessible title for the map (becomes an `aria-label`).\n- **legend** - Visual legend rendered alongside the map.\n- **marker-icon** - Template for custom marker icons. Cloned per point.",
      "attributes": [
        {
          "name": "center",
          "description": "Initial center as `\"lat,lng\"` (e.g. `52.37,4.89`).",
          "values": []
        },
        { "name": "zoom", "description": "Initial zoom level.", "values": [] },
        {
          "name": "min-zoom",
          "description": "Minimum zoom level allowed.",
          "values": []
        },
        {
          "name": "max-zoom",
          "description": "Maximum zoom level allowed.",
          "values": []
        },
        {
          "name": "tile-url",
          "description": "Tile URL template. Defaults to OpenStreetMap.",
          "values": []
        },
        {
          "name": "tile-attribution",
          "description": "HTML attribution string for the tiles.",
          "values": []
        },
        {
          "name": "tile-subdomains",
          "description": "Comma-separated subdomain list for the tile URL template.",
          "values": []
        },
        {
          "name": "cluster",
          "description": "Group nearby markers into clusters.",
          "values": []
        },
        {
          "name": "fit-bounds",
          "description": "Auto-zoom to fit all points/regions on load.",
          "values": []
        },
        {
          "name": "interactive",
          "description": "Enable pan/zoom (default true).",
          "values": []
        },
        {
          "name": "scroll-wheel-zoom",
          "description": "Enable scroll-wheel zoom.",
          "values": []
        },
        { "name": "data-theme", "values": [] }
      ],
      "references": []
    },
    {
      "name": "cui-modal",
      "description": "Modal dialog built over the native `<dialog>` element. Backdrop dismiss,\nESC handling, and focus management come from the platform; this element\nadds attribute control, an open/close JS API, typed close events, and\nremote triggering via `data-cui-modal-target` from anywhere on the page.\n---\n\n\n### **Events:**\n - **cui-modal-open** - Fires after the dialog opens.\n- **cui-modal-close** - Fires after the dialog closes. `detail.returnValue` carries the dialog's `returnValue` (set via `close(value)` or `<form method=\"dialog\">`).\n- **cui-modal-cancel** - Fires when the user cancels via ESC or backdrop click (before `cui-modal-close`).\n\n### **Slots:**\n - _default_ - The dialog body. Wrap with a `<div>` or semantic landmark; include a close affordance (any element with `data-cui-modal-close`) for users who can't dismiss via ESC.",
      "attributes": [
        {
          "name": "open",
          "description": "Reflects the dialog's open state. Set or remove to open/close imperatively.",
          "values": []
        }
      ],
      "references": []
    },
    {
      "name": "cui-navbar",
      "description": "Responsive site navigation with collapsible mobile panel, nested dropdowns,\nand optional sticky positioning. The element renders no styling of its own —\nit enhances light-DOM markup built from the `.cui-navbar` class family,\ninjecting the burger toggle button and wiring up collapse, dropdown, sticky,\nand ARIA behavior. All brand/nav/action content stays in the light DOM so it\nremains semantic, searchable, and SEO-friendly.\n\nThe burger button is the one piece of chrome the component owns: it is\ncreated and ARIA-wired by the element rather than authored, so the\n`aria-expanded` / `aria-controls` contract can't be mis-authored per page.\nEverything else is plain `.cui-navbar-*` markup.\n---\n",
      "attributes": [
        {
          "name": "expanded",
          "description": "Mobile collapse panel state. Toggled by the injected burger button; can also be set programmatically.",
          "values": []
        },
        {
          "name": "sticky",
          "description": "Enables `position: sticky` on the navbar.",
          "values": []
        },
        {
          "name": "sticky-offset",
          "description": "CSS length used as `top` while sticky (e.g. `0`, `var(--cui-space-2)`).",
          "values": []
        },
        {
          "name": "sticky-z-index",
          "description": "CSS `z-index` while sticky.",
          "values": []
        }
      ],
      "references": []
    },
    {
      "name": "cui-scroll-stage",
      "description": "Scroll-driven \"stage\" element that publishes its focus/offset/active state\nas CSS custom properties on itself (and optionally on a tone target like\n`:root`), so child layouts and parallax effects can react via CSS alone.\nPair with `cui-reveal` for entry animations or with the exported\n`attachCuiParallax()` helper for parallax bindings.\n---\n\n\n### **Slots:**\n - _default_ - Stage content. Typically a sticky inner region plus the layers that move with scroll progress.",
      "attributes": [
        {
          "name": "focus-bias",
          "description": "Where in the viewport the stage is considered \"in focus\". CSS length or unit-less ratio.",
          "values": []
        },
        {
          "name": "hold",
          "description": "How long to keep the stage at full focus before easing out, as a CSS length or ratio.",
          "values": []
        },
        {
          "name": "threshold",
          "description": "Minimum viewport overlap before the stage starts publishing focus (0–1).",
          "values": []
        },
        {
          "name": "tone",
          "description": "Tone value to publish on the tone target while the stage is active (e.g. `dark`, `inverse`).",
          "values": []
        },
        {
          "name": "tone-target",
          "description": "CSS selector that should receive the tone attribute. Accepts `document` or `:root` to target the root element.",
          "values": []
        },
        {
          "name": "sticky-top",
          "description": "CSS `top` value applied to the inner sticky region.",
          "values": []
        },
        {
          "name": "min-block-size",
          "description": "Minimum block-size of the stage; controls how much scroll the stage consumes.",
          "values": []
        }
      ],
      "references": []
    },
    {
      "name": "cui-toast-region",
      "description": "Container for transient toast notifications. Usually created on demand by\nthe exported `toast()` function — you only place this element yourself if\nyou want to control placement or pre-render a region for SSR. Each region\nis identified by its `placement`; calls to `toast()` are routed to the\nregion matching their requested placement, or a new region is created.\n---\n\n\n### **Slots:**\n - _default_ - Toast elements. Normally populated by `toast()`; manual children are allowed and remain in DOM until dismissed.",
      "attributes": [
        {
          "name": "placement",
          "description": "Region anchor point. Default `block-end-inline-end` (bottom-right).",
          "values": [
            { "name": "block-start-inline-start" },
            { "name": "block-start-inline-end" },
            { "name": "block-end-inline-start" },
            { "name": "block-end-inline-end" },
            { "name": "block-start-center" },
            { "name": "block-end-center" }
          ]
        }
      ],
      "references": []
    },
    {
      "name": "cui-theme-toggle",
      "description": "Button that cycles the page theme through `auto`, `light`, and `dark`. The\nchoice is persisted in `localStorage` under `cui-theme` and read at boot\n(see the inline script in the docs `<head>`). Pairs with the exported\n`getTheme()` / `setTheme()` helpers for programmatic control.\n---\n",
      "attributes": [
        {
          "name": "label-auto",
          "description": "Visible label when in auto mode.",
          "values": []
        },
        {
          "name": "label-light",
          "description": "Visible label when in light mode.",
          "values": []
        },
        {
          "name": "label-dark",
          "description": "Visible label when in dark mode.",
          "values": []
        },
        {
          "name": "aria-label-auto",
          "description": "Accessible name when in auto mode.",
          "values": []
        },
        {
          "name": "aria-label-light",
          "description": "Accessible name when in light mode.",
          "values": []
        },
        {
          "name": "aria-label-dark",
          "description": "Accessible name when in dark mode.",
          "values": []
        }
      ],
      "references": []
    },
    {
      "name": "cui-tabs",
      "description": "Tabbed interface with ARIA `tablist`/`tab`/`tabpanel` roles and full\nkeyboard support (Left/Right, Home/End). The element renders no styling of\nits own — it enhances light-DOM markup built from the `.cui-tabs`,\n`.cui-tablist`, and `.cui-tab` classes, wiring up `id`, `aria-controls`,\n`aria-selected`, roles, and panel visibility. All panels stay in the light\nDOM so crawlers see every panel regardless of which tab is active.\n\nTabs are the `.cui-tab` controls inside the `.cui-tablist`; panels are the\nremaining direct children of the element, paired with the tabs by source\norder. Mark the initially-open tab with `aria-selected=\"true\"` (defaults to\nthe first tab).\n---\n",
      "attributes": [],
      "references": []
    },
    {
      "name": "cui-tree",
      "description": "Hierarchical tree with selection, expand/collapse, keyboard navigation,\nand optional drag-and-drop reordering. Each item is a light-DOM\n`.cui-tree-item` with a unique `data-id`; nested `<ul>`s become subtrees.\nConsumers handle persistence — the element fires events but does not\nmutate item structure on its own.\n---\n\n\n### **Events:**\n - **cui-tree-item-select** - Fires when an item is selected. `detail.id` is the selected `data-id`.\n- **cui-tree-contextmenu** - Fires on right-click / context-menu key. Call `detail.preventDefault()` to suppress the native menu and render your own.\n- **cui-tree-dragstart** - Fires when a drag begins.\n- **cui-tree-drop** - Fires on a valid drop. `detail.position` is `before`, `after`, or `inside`. Consumers apply the structural change in their event handler.\n- **cui-tree-expand** - Fires when a subtree expands.\n- **cui-tree-collapse** - Fires when a subtree collapses.\n\n### **Slots:**\n - _default_ - Tree markup: nested `<ul>` / `<li class=\"cui-tree-item\">` with `data-id` on each item and optional `.cui-tree-toggle` controls for expand/collapse.",
      "attributes": [
        {
          "name": "selectable",
          "description": "Whether items can be selected. Default true.",
          "values": []
        },
        {
          "name": "draggable-items",
          "description": "Enables drag-and-drop reordering. Consumers must handle the resulting `cui-tree-drop` event to persist the new structure.",
          "values": []
        }
      ],
      "references": []
    },
    {
      "name": "cui-reveal",
      "description": "Reveals its content with an entry animation as it scrolls into view, using\r\n`IntersectionObserver` under the hood. Honors `prefers-reduced-motion` —\r\nusers with reduced motion see the content immediately, no transform.\n---\n\n\n### **Slots:**\n - _default_ - Content to reveal. Any block-level content works.",
      "attributes": [
        {
          "name": "delay",
          "description": "Delay before the reveal starts. Accepts a number (milliseconds) or any CSS time value (`200ms`, `0.3s`).",
          "values": []
        },
        {
          "name": "distance",
          "description": "Translate distance for the entry transform. Accepts a number (pixels) or any CSS length (`24px`, `2rem`).",
          "values": []
        },
        {
          "name": "threshold",
          "description": "`IntersectionObserver` threshold (0–1).",
          "values": []
        }
      ],
      "references": []
    }
  ]
}
